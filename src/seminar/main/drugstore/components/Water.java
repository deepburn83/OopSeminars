package seminar.main.drugstore.components;

import seminar.main.drugstore.Component;

public class Water extends Component {
    public Water(String title, Double weight, int power) {
        super(title, weight, power);
    }
}
